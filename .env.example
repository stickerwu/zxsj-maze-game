# 噩梦潜渊古龙陵萧行云P2内场模拟器 - 环境变量配置
# 复制此文件为 .env 并根据需要修改配置

# ===========================================
# Docker 配置
# ===========================================

# Docker Hub 用户名（用于镜像推送）
DOCKER_USERNAME=your-dockerhub-username

# Docker 镜像标签
TAG=latest

# Docker 容器端口映射
DOCKER_PORT=9898

# ===========================================
# GitHub 配置
# ===========================================

# GitHub 仓库地址
GITHUB_REPO=your-github-username/zxsj-maze-game

# ===========================================
# 项目配置
# ===========================================

# 项目名称
PROJECT_NAME=zxsj-maze-game

# 环境模式
NODE_ENV=production

# ===========================================
# 部署配置
# ===========================================

# 是否启用健康检查
HEALTH_CHECK_ENABLED=true

# 健康检查间隔（秒）
HEALTH_CHECK_INTERVAL=30

# 容器重启策略
RESTART_POLICY=unless-stopped

# ===========================================
# 网络配置
# ===========================================

# Docker 网络名称
NETWORK_NAME=zxsj-maze-network

# ===========================================
# 说明
# ===========================================

# 1. DOCKER_USERNAME: 你的 Docker Hub 用户名，用于推送镜像
# 2. TAG: Docker 镜像标签，通常使用 latest 或版本号
# 3. DOCKER_PORT: 容器对外暴露的端口号
# 4. GITHUB_REPO: GitHub 仓库地址，格式：用户名/仓库名
# 5. PROJECT_NAME: 项目名称，用于容器命名
# 6. NODE_ENV: Node.js 环境模式，生产环境使用 production
# 7. HEALTH_CHECK_ENABLED: 是否启用容器健康检查
# 8. RESTART_POLICY: 容器重启策略，推荐使用 unless-stopped
# 9. NETWORK_NAME: Docker 网络名称，用于容器间通信

# ===========================================
# 使用方法
# ===========================================

# 1. 复制此文件：cp .env.example .env
# 2. 编辑 .env 文件，设置你的配置
# 3. 运行部署脚本：./scripts/deploy.sh --run
# 4. 或使用 docker-compose：docker-compose up -d

# ===========================================
# 注意事项
# ===========================================

# 1. .env 文件包含敏感信息，不要提交到版本控制系统
# 2. 确保 DOCKER_USERNAME 与你的 Docker Hub 账户一致
# 3. DOCKER_PORT 确保不与其他服务冲突
# 4. 生产环境建议使用具体的版本标签而不是 latest